# Data Layout

- `data/seeds`: seed inputs checked into the repo
- `data/configs`: static runtime configuration files
- `data/models`: generated model artifacts safe for Go runtime loading

These directories are intentionally empty in the bootstrap state.

